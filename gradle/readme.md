# Gradle

system for buidling, CI, testing and automating

## Version library management

* update the version catalog `/gradle/libs.versions.toml`

    ```bash
    ./gradlew versionCatalogUpdate --no-configuration-cache
    ```